import React from "react";

import "./header.css";

const Header = (props) => {
  return (
    <header className="header">
      <h2 className="title">Block Explorer</h2>
    </header>
  );
};

Header.propTypes = {};

export default Header;
